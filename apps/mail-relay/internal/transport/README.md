# transport/

SOCKS proxy rotation, pool management, onion bridge, traffic shaping, VPN.

**M2.3 target layout:**
- `transport/` — proxy pool core (already here ✅)
- `transport/onion/` — ← will move from `../onion/`
- `transport/bridge/` — ← will move from `../bridge/`
- `transport/pool/` — ← will move from `../pool/`
- `transport/traffic/` — ← will move from `../traffic/`
- `transport/vpn/` — ← will move from `../vpn/`
- `transport/fragment/` — ← will move from `../fragment/`
- `transport/decoy/` — ← will move from `../decoy/`
- `transport/constrate/` — ← will move from `../constrate/`
- `transport/metamin/` — ← will move from `../metamin/`
