# delivery/

Outbound SMTP envelope rewrite, sanitization, content encoding.

**M2.4 target layout:**
- `delivery/` — already here ✅
- `delivery/sanitizer/` — ← will move from `../sanitizer/`
- `delivery/contentenc/` — ← will move from `../contentenc/`
