# TEMPLATE: OpenAPI 3.1 kostra. Nahraď <placeholder> hodnoty a doplň paths/components.
# TEMPLATE: Tento soubor je zdrojem pravdy pro REST kontrakty. Generátor klientů + serverů.

openapi: "3.1.0"

info:
  title: "<service-name> API"
  version: "0.1.0"
  description: |
    Public REST API pro doménu `<service-name>`.
    Viz service.yaml pro ownership, dependencies a invarianty.
  contact:
    name: "<owner>"

servers:
  - url: "http://localhost:8080"
    description: Local dev
  - url: "https://<service-name>.railway.app"
    description: Production

security:
  - apiKey: []

paths: {}
  # TEMPLATE: přidej endpointy, např.:
  #
  # /api/<service>/health:
  #   get:
  #     summary: Health check
  #     security: []
  #     responses:
  #       '200':
  #         description: OK
  #         content:
  #           application/json:
  #             schema:
  #               $ref: '#/components/schemas/HealthResponse'

components:
  securitySchemes:
    apiKey:
      type: apiKey
      in: header
      name: X-API-Key

  schemas: {}
    # TEMPLATE: přidej schémata, např.:
    #
    # HealthResponse:
    #   type: object
    #   required: [status]
    #   properties:
    #     status:
    #       type: string
    #       enum: [ok, degraded, down]
    #     timestamp:
    #       type: string
    #       format: date-time
