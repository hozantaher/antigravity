.claude/worktrees/
.claude/scheduled_tasks.lock
.claude/settings.local.json
node_modules/
*.log
.DS_Store
_archive/
# JS app (Firebase-hosted, not a Railway Go service) — its pnpm store symlinks
# point into an external node_modules and break `railway up` indexing. Go builds
# (services/*, modules/*) never need apps/.
apps/
