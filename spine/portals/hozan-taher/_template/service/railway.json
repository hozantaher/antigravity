{
  "_TEMPLATE_note": "kopíruj do services/<domain>/railway.json a uprav. Alternativně Railway akceptuje railway.toml (viz services/anti-trace-relay/railway.toml pro příklad).",
  "_TEMPLATE_docs": "https://docs.railway.app/reference/config-as-code",
  "build": {
    "builder": "DOCKERFILE",
    "dockerfilePath": "Dockerfile"
  },
  "deploy": {
    "healthcheckPath": "/healthz",
    "healthcheckTimeout": 30,
    "restartPolicyType": "ON_FAILURE",
    "restartPolicyMaxRetries": 3,
    "numReplicas": 1
  }
}
