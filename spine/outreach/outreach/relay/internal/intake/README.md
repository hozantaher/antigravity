# intake/

HTTP ingestion surface: /submit, /v1/proxy-pool, auth, duress, admin.

**M2.4 target layout:**
- `intake/` — already here ✅
- `intake/httpapi/` — ← will move from `../httpapi/`
- `intake/auth/` — ← will move from `../auth/`
- `intake/duress/` — ← will move from `../duress/`
- `intake/admin/` — ← will move from `../admin/`
