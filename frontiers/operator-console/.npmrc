# Firebase App Hosting buildpack / Cloud Run: pnpm symlinks se lamou
# v Docker image. node-linker=hoisted vytvoří flat node_modules (jako npm)
# takže express a ostatní deps jsou přímo dosažitelné bez symlinků.
node-linker=hoisted
