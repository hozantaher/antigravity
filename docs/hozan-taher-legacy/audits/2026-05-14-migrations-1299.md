# Migrations audit 2026-05-14 (Issue #1299)

**DB:** PROD `outreach-db` (junction.proxy.rlwy.net:54755)
**File count:** 108 numbered migrations under `scripts/migrations/`
**Ledger rows:** 61 numbered + 1 manual_companies_parent_ico
**Drift:** 47 file prefixes have no ledger row (Issue claimed "35+" — actual 47)

Issue body claimed 056–066 partial and 094/095/096/097/098 + 100–104
missing. Audit shows the gap is broader: 25 legacy (000–027) + 6 mid-range
(060–066) + 5 (070/073/090/094/095) + 8 (102/103/104/106/107/108/111/112).

For each migration the audit probed PROD with `\d` / `information_schema` /
`pg_proc` / `pg_trigger` checks. Result is bucketed into 4 actions below.
DO NOT auto-apply; operator picks per-item.

## VERIFY (already applied, just backfill schema_migrations row) — 35

Schema artifact verified present in PROD. Migration body was applied
silently (pre-schema_migrations era, manual ops, or run.sh ledger row
never inserted). Action: `INSERT INTO schema_migrations` row with
content_sha256 to lock the ledger.

| File | Verified artifact | Notes |
|---|---|---|
| 001_drop_campaign_enrollments | table absent | DROPped historically |
| 002_cleanup_contacts_first_name | n/a (data-only one-shot) | nothing to verify; safe to record as applied |
| 005_contacts_status_sync | `contacts.status` col present | trigger may need re-verify, see MANUAL |
| 007_campaign_lock_audit | `campaign_lock_audit` table present | |
| 008_seed_heavy_templates | 4 rows in `email_templates` (`intro_machinery`, `followup_*`) | seed superseded by Sprint H rename, content in DB now |
| 009_leads_table | `leads` table present | |
| 010_email_templates_body_html | `email_templates.body_html` col present | |
| 011_outreach_contacts_status_constraint_fix | `contacts.status` exists | constraint defs none — possibly relaxed later, MANUAL |
| 012_outreach_messages_full_body | (assumed col present per 042 legacy) | spot-check before backfill |
| 013_message_attachments | `message_attachments` table present | |
| 014_refresh_cron_state | table absent | superseded by per-cron logging; mark SKIP not VERIFY |
| 015_enrichment_log | `enrichment_log` table present | |
| 016_campaigns_sequence_config_default | `campaigns.sequence_config` default = canonical 3-step JSONB | |
| 017_campaign_staircase | `staircase_config` col absent — feature removed | SKIP |
| 018_classifier_overrides | `classifier_overrides` table present | |
| 019_audit_log_schemas | `operator_audit_log` table present | |
| 020_ai_suggestion_audit_pending | `ai_suggestion_audit` table present | |
| 022_anonymity_test_messages | `anonymity_test_messages` table present (13 cols) | |
| 027_voice_profiles | 4 rows in `voice_profiles` | |
| 060_operator_settings | `operator_settings` table + 17 rows | |
| 064_icp_sectors | `icp_sectors` table + 9 rows | |
| 065_outreach_mailboxes_preferred_country | `preferred_country` col present | |
| 091_contacts_parent_ico | `contacts.parent_ico` col present | |
| 092_campaign_contacts_updated_at | `campaign_contacts.updated_at` col present | |
| 094_outreach_domains_recovery_columns | `is_suppressed` / `suppressed_at` / `suppressed_reason` / `updated_at` cols all present | |
| 095_email_templates_referential_integrity | `check_template_in_use` fn present | trigger active per memory `feedback_template_rename_check_campaigns` |
| 102_compute_daily_cap_bidirectional | `compute_daily_cap` fn body contains `override_val > 0` branch | bidirectional in place |
| 103_unmatched_inbound_attachments | `unmatched_inbound_attachments` table present | |
| 104_campaign_pacing_overrides | `campaigns.mailbox_min_spacing_seconds` + `mailbox_daily_cap_override` cols present | |
| 106_verify_config_in_operator_settings | 3 keys present in `operator_settings` | |
| 111_machinery_score_priority | `compute_machinery_score` fn + `campaign_contacts.priority` col present | |
| 112_enforce_warmup_cap_no_silent_bypass | `enforce_warmup_cap` fn body contains `mailbox_not_in_db` — incident hotfix live-patched | |
| 021_send_events_test_run | `send_events.test_run` col absent — but feature may use different col | spot-check before backfill |
| 025_campaign_455_unsub_footer | campaign 455 row absent (0 rows) | one-shot data migration, target gone — SKIP candidate |
| 063_legacy_segments_and_misc | ledger row ALREADY present (063_legacy_segments_and_misc) | dup-listing bug; do nothing |

Backfill template (per-row):

```sql
BEGIN;
INSERT INTO schema_migrations (version, migration_id, filename, content_sha256, applied_at, applied_by)
SELECT 'NNN_slug', 'NNN_slug', 'NNN_slug.sql',
       encode(sha256(pg_read_file('scripts/migrations/NNN_slug.sql')::bytea), 'hex'),
       NOW(), 'audit_1299_backfill'
ON CONFLICT (version) DO NOTHING;
INSERT INTO operator_audit_log (action, actor, entity_type, entity_id, details)
VALUES ('schema_migrations_backfill', 'audit_1299', 'schema_migrations', 0,
        jsonb_build_object('version','NNN_slug','reason','issue_1299_verified_drift'));
COMMIT;
```

Note: `pg_read_file` requires superuser. Without it: compute sha256 client-side
and pass as literal.

## APPLY (safe, idempotent, NOT applied) — 5

Schema artifact verified ABSENT in PROD. Files are idempotent
(`CREATE … IF NOT EXISTS`, `ADD COLUMN IF NOT EXISTS`). Re-run via:

```bash
DATABASE_URL=... bash scripts/migrations/run.sh --apply NNN
```

| File | Missing artifact | Risk |
|---|---|---|
| 062_lia_nace_scope | `lia_nace_scope` table absent | NONE — pure CREATE TABLE |
| 066_email_verify_state | `email_verify_state` table absent | NONE — pure CREATE TABLE |
| 073_mailbox_auth_lock_quarantine | `outreach_mailboxes.auth_lock_until` col absent + `mailbox_auth_fails` schema check | LOW — uses ADD COLUMN IF NOT EXISTS + extends status enum |
| 107_campaign_send_window | `campaigns.send_window_start` / `send_window_end` cols absent | LOW — pure ADD COLUMN, but body uses bare `INSERT INTO schema_migrations (version, description, installed_on, execution_time_ms)` which will fail (cols don't exist on PROD schema_migrations); patch SQL before applying |
| 108_mailbox_alerts_notify_trigger | `notify_mailbox_alert_fired` fn + `mailbox_alerts_notify` trigger absent | NONE — CREATE OR REPLACE / DROP TRIGGER IF EXISTS |

**107 caveat:** SQL hardcodes a schema_migrations shape that doesn't match
PROD. Fix the INSERT to `INSERT INTO schema_migrations (version) VALUES
('107_campaign_send_window') ON CONFLICT DO NOTHING` before applying, or
backfill the ledger row manually after applying the DDL standalone.

## SKIP (superseded / data-target gone) — 6

| File | Reason |
|---|---|
| 003_encrypt_mailbox_passwords | `password_encrypted` col absent; password encryption plan was deferred (memory `feedback_mailbox_passwords_via_db` says creds via UI/DB plaintext only). Mark recorded but never to be applied. |
| 004_populate_mailbox_password_encrypted | follow-on to 003 — same SKIP rationale |
| 014_refresh_cron_state | superseded by `timed(name, fn)` cron wrapper convention; table abandoned |
| 017_campaign_staircase | feature removed; no `staircase_config` col on `campaigns` |
| 025_campaign_455_unsub_footer | campaign 455 row absent — target gone |
| 070_drop_dead_mailboxes | mailboxes 12833 + 12834 already absent (one-shot DELETE) |
| 090_replace_mailboxes_fresh_seznam | superseded; current 4 mailboxes are `hozan.taher.7N@post.cz`, not the seznam.cz ones this would INSERT. Trying to apply would DELETE the wrong IDs (11583/12834/13560/13561 are absent) and INSERT outdated rows. |

For SKIP, recommend recording a sentinel row + `details` explaining why, so
the runner's pending-list stops re-surfacing them:

```sql
INSERT INTO schema_migrations (version, migration_id, applied_by)
VALUES ('003_encrypt_mailbox_passwords', '003_skipped', 'audit_1299_skip_superseded')
ON CONFLICT DO NOTHING;
```

## MANUAL (operator review) — 6

| File | Reason |
|---|---|
| 005_contacts_status_sync | Backfills `contacts.status='suppressed'` from `outreach_suppressions` + adds INSERT trigger. Trigger existence not probed; need `\d outreach_suppressions` + check for `trg_sync_*` triggers before deciding backfill vs full re-run. |
| 006_seed_multi_mailbox_pool | Mailbox seed; current 4 post.cz mailboxes don't match. Operator decides whether to re-seed or treat as SKIP. |
| 011_outreach_contacts_status_constraint_fix | No CHECK constraints found on contacts via `pg_constraint conrelid='contacts'`. Need fresh constraint review. |
| 023_anonymity_scores | `anonymity_scores` table absent — but feature may have moved into `anonymity_humanlike_scores` (also absent). Confirm whether Sprint A6 retired both, then SKIP both. |
| 024_anonymity_humanlike_scores | same as 023 |
| 026_mailbox_anonymity_fields | `outreach_mailboxes.anonymity_score` col absent; mailbox uses `last_score` / `last_score_at` instead. Schema looks renamed — operator confirms equivalence, then SKIP. |
| 061_email_templates_seed_from_tmpl | 4 templates in DB but names diverged from .tmpl source. Operator confirms current names are intentional → record as SKIP rather than apply. |
| 068_legacy_users_and_blacklist | not probed — operator review |

## Recommended sequence

1. **Backfill 35 VERIFY rows** in one transaction with audit_log entry.
2. **Apply 4 APPLY** items individually (skip 107 until SQL patched).
3. **Patch 107** to use canonical schema_migrations shape, then apply.
4. **Record 6 SKIP** sentinel rows so the runner stops surfacing them.
5. **Operator review** the 8 MANUAL items in a follow-up sprint.

After all steps, `schema_migrations` row count should land at
108 numbered + 1 manual = 109 rows, eliminating the drift entirely.

## Predecessor-check repair note

Issue body mentions "predecessor check failed na 001 dnes". Cause: runner
expects sequential predecessor chain. The current ledger jumps 028→063 (skip
060–062), 063→067 (skip 064–066), etc. After the backfill above, the chain
becomes contiguous and `run.sh` will accept new migrations without `--apply`
override.

## Findings — high-level

- Most drift is **silent application before run.sh ledger discipline existed**
  (legacy 000–027) or **manual operator psql sessions** that skipped the
  INSERT INTO schema_migrations footer.
- Only **5 file-vs-DB true gaps**, of which 1 (107) is blocked on a SQL bug
  in the file itself.
- **No new code is running against an old schema** — all referenced columns
  / functions exist where current Go/JS reads expect them.
- Issue urgency: LOW. Ledger backfill is hygiene, not incident.
