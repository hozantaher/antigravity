# Stories backlog — ~300 scenarios (#1586 follow-on)

> **Status:** Active (auto-approved per operator 2026-06-02) · **Trigger:** operator —
> "vygeneruj 300 nových stories … big-win prioritně, poté low-hanging a mid …
> žádné big-plans … schval hned a pracuj autonomně … 92 % coverage … odstraňuj
> dead-code". Generated by 4 parallel Explore agents across the system.
>
> Each story is single-PR-sized (no multi-sprint plans). Execution order:
> **BIG-WIN → LOW-HANGING → MID**, interleaving dead-code removal. Safe units only
> while operator asleep (no auto-send, no prod deletes). Coverage target 92%.

## How to use
Pull the top BIG-WIN that is safe + concrete, implement as 1 verified commit
(build + test + smoke where UI), then the next. Remove superseded/dead code as
encountered ([[feedback_remove_dead_code_regularly]]). This doc is the working
backlog; tick items in commit messages (refs the area).

---

## AREA 1 — Inbound triage + email mining

**BIG-WIN:** multi-phone extraction + confidence ranking · exclude phones in quoted history · intl phone (+421/+43/+49/+48) · auto-link reply→contact by mined phone (migration) · price RANGES · "kvalitní signály" filter · duplicate-reply chip (same sender <1h) · "waiting X days" hot-lead badge · reply→vehicle contact edge.
**LOW:** test phone in whitespace/NBSP noise · parseSignature null when only-quote · htmlToText numeric entities (€) · disable send on empty draft · ChatThread "nová konverzace" empty state · test 'c' no-op without phone · quoteStrip mixed CRLF · MinedSignals cap prices >3 · factsRow null-signature · testid on reply-fallback button · cid img → "[foto]" placeholder · CZK uppercase = Kč.
**MID:** phone confidence scoring · signature confidence (high/medium) · price dedup across formats · signature-from-quoted detection · legal-form normalization · stripQuotedReply boundary tests · phantom-vehicle guard (unquoted only) · location confidence (exact vs stem) · IČO→ARES name · signature override UI · attachment fetch graceful fallback · trusted-sender flag · mined-field validation in BFF · reply context search (subject+phone+company).

## AREA 2 — Data graph (vehicles · companies · contacts · CRM · search)

**BIG-WIN:** dedup contacts by email+phone · dedup companies by IČO · auto-link contact→CRM on email match · resolve contact.company_name→company_id FK · phone search in contacts · SPZ+phone search in vehicles · IČO search in companies · search rank by link degree · flag orphan FKs (vehicle.contact_id, contact.company_id) · enrich vehicle contact with mined phone.
**LOW:** empty-state illustration on Vozidla · company ICP tier on vehicle detail · clickable company→Firmy link · SPZ in vehicle title · pluralize "Vozidla (N)" · email-status icon on contact row · "5× osloven" badge · CRM relationship full label · search skeleton · disable buttons during PATCH · "no vehicles/contacts" hints · search-404 "entita smazána".
**MID:** batch verify company emails · enrich contact phone from CRM · IČO mining from reply · phone normalize on write · "has vehicles/replies" filters · price-range filter · email-status filter · sort vehicles by price · recent replies on company detail · contact→vehicles section in CRM · cross-link breadcrumbs · vehicleTitle null test · contract test vehicles POST crm-upsert · integration test company IČO dedup · e2e vehicle→contact→CRM chain · audit orphan-FK ratchet.

## AREA 3 — Campaigns · sending · mailboxes · provoz

**BIG-WIN:** bounce-rate trend (7d/30d) on HaltAdvisory · preflight refuse resume if mailbox auth_locked · DNS audit UI (#1585/#1321) · halt-threshold editor panel · warmup "won't send until phase N" banner · send-window heatmap · stale in_flight reclaim status (AV-F9) · mailbox credential-crisis page · hard/soft bounce breakdown · relay pool capacity warning · send-test-via-relay button · halt recommendation banner · auto preflight re-run after unlock · send-window red/yellow/green dot · bounce-profile snapshot.
**LOW (sel):** render null sequence_config gracefully · FBL-unavailable note on complaint_rate · "updated X ago" on mailbox rows · empty-state V2Kampane · stale-config typo guard · relay health quick-check · known-SMTP-error help · dedup-guard stats card.
**MID (sel):** rate-limit status w/ countdown · presend_skip reason breakdown · mailbox CSV export · cron heartbeat dashboard · lifecycle cap thresholds in operator_settings · email-quality trend · config-drift summary · suppression audit modal · template quality-gate (AR2/AR5) checks · bounce bulk-skip by domain.

## AREA 4 — Data quality · error-handling · coverage (→92%) · dead-code

**BIG-WIN:** new DQ check `contacts_without_phone` · `stale_company_facts` (>90d) · `reply_mined_incomplete` · unit tests for `/api/data-quality` + fix endpoints · cron tests (28 crons, ~6 tested — prioritize outboundReply/imapPoll/mailboxHealing/vehicleAutoCapture) · error-path tests for untested routes · contract test v2 data endpoints · monkey-route coverage (v2/v3 + missing routes) · e2e data-quality flow.
**MID:** error-path tests for computeDataQuality/stalenessFactor · replies has_phone filter tests · repliesStats/operatorMetrics/dashboardSummary null tests · useResource hook tests · v2 page error-boundary tests.
**LOW (test-adds):** ~40 per-route/lib unit tests (replyTime, widgetFormatters, filterSerializer, cohort, suppression union, search, templates, categoryTree, mailbox* stats, funnelSummary, etc.).

### DEAD CODE FOUND (verify-then-remove)
- `src/components/page/PageFooter.jsx` + its `index.js` export — appears unused.
- `src/server-routes/leads.js` / `/api/leads` — leads table vestigial (CLAUDE.md "leady JSOU vozidla"); audit refs then remove.
- stale `/leads`/`/priprava` nav/test references.
- audit `src/lib/heal-*.js`, `src/lib/stale.js` for uncalled helpers.
- ~10 e2e specs referencing removed `/priprava` (iter49/50/51, full-ui-sweep, iter62-monkey ROUTES) — trim/repoint.

---

**Execution log:** see commits refs #1586 (area tag in message). Big-wins + safe
test-coverage + dead-code removal proceed autonomously; product/destructive items
(auto-send, prod deletes, targeting flips) wait for operator.
